// SPDX-License-Identifier: PMPL-1.0-or-later
= typed-wasm Roadmap
:author: Jonathan D.A. Jewell
:revdate: 2026-03-29

== Current State

typed-wasm applies a checked TypeLL-derived 10-level core to WebAssembly linear
memory. Pre-alpha / research stage. Core insight: WASM linear memory is a
schemaless database; typed-wasm adds region schemas and typed access
operations.

Killer feature: multi-module shared memory type safety across language boundaries
(e.g. Rust Module A shares WASM memory with ReScript Module B -- neither source
type system covers the boundary; typed-wasm does).

Key capabilities delivered:

* Idris2 ABI proofs (src/abi/) -- checked L1-L10 region, access, and proof core
* Zig FFI (ffi/zig/) -- C-ABI bridge for runtime region management, typed load/store
* `.twasm` grammar (spec/grammar.ebnf) -- EBNF surface syntax
* 10-level spec (spec/10-levels-for-wasm.adoc) -- checked DB-to-WASM core mapping
* Example `.twasm` files for the checked core, plus draft higher-level examples
* Whitepaper drafts (docs/WHITEPAPER.md + docs/arxiv/typed-wasm.tex) under audit
* No unsound escape hatches in the checked Idris2 core (`believe_me`, `assert_total`)

Known audit constraints:

* L11-L12 draft files are not part of `typed-wasm.ipkg` and currently fail standalone type-checking
* Release/build/container metadata still contains template residue
* Benchmark and venue-facing paper claims require evidence cleanup before submission

== v0.1.0 -- Formal Foundations (Complete)

* [x] Idris2 ABI: region schema proofs
* [x] Idris2 ABI: typed access proofs
* [x] Idris2 ABI: checked 10-level verification hierarchy
* [x] Zig FFI: runtime region management
* [x] Zig FFI: typed load/store operations
* [x] `.twasm` EBNF grammar
* [x] 10-level specification document
* [x] Example `.twasm` files
* [x] Multi-module shared memory type definitions

== v0.2.0 -- ECHIDNA Integration (Complete — 2026-03-29)

* [x] Property-based testing of proof soundness via ECHIDNA Prover Wars
* [x] Random `.twasm` program generation (tests/echidna/echidna-harness.mjs)
* [x] Prover oracle testing (ffi/zig/test/echidna_oracle_test.zig — 7 properties)

== v0.3.0 -- Submission Drafts (In revision)

* [x] Formalise whitepaper for arXiv submission (docs/arxiv/typed-wasm.tex — ACM sigplan format)
* [x] BibTeX bibliography (docs/arxiv/typed-wasm.bib — 15 references)
* [ ] Benchmark multi-module type checking overhead with reproducible artefacts
* [x] Comparison with existing WASM safety approaches (Section 8, Table 6)
* [ ] Reconcile all paper claims with the checked L1-L10 evidence envelope
* [ ] Submit to arXiv / HAL only after claim and benchmark audit passes

== v0.4.0 -- Ecosystem Integration (Partially Complete)

* [x] TypedQLiser plugin (WASM as a "query target") — typedqliser/src/plugins/wasm.rs (541 lines)
* [ ] VQL-UT sibling integration (same levels, different domain)
* [ ] GraalVM/Truffle target (multi-language shared state type safety)

== v1.0.0 -- Stable Release

* [ ] Complete `.twasm` parser (beyond EBNF grammar)
* [ ] End-to-end pipeline: `.twasm` source -> verified WASM module
* [ ] Multi-language binding generation from typed region schemas
* [ ] CI/CD with proof validation
* [ ] De-template release/build/container surfaces
* [ ] Real benchmark and aspect-test evidence

== Future

* GraalVM compile-time guarantees replacing runtime InteropLibrary dispatch
* Browser-side `.twasm` validation tooling
* Language server protocol support for `.twasm` files
